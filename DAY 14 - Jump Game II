45. Jump Game II
class Solution {
    public int jump(int[] nums) {
     int n=nums.length;
     int i=0,step=0,c=0;
      for(int j=0;j<n-1;j++){
         step=Math.max(step,j+nums[j]);
        if(j==i){
            c++;
            i=step;
        }
       }
       return c;
    }
}
